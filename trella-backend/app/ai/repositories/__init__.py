"""AI repositories package.

Intentionally empty in Phase 0. Kept so the AI module mirrors the project's
Repository -> Service -> Router architecture, and so future persistence
(conversation history, AI usage, cost tracking, feedback) can be added here
without restructuring the module. See ``.ai/PHASE_0_PLAN.md``.
"""
